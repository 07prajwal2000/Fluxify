export const STARTERS = [
	{ label: "CRUD API + JWT", prompt: "Generate a blog API with rate limiting, Redis cache and JWT auth." },
	{ label: "Stripe webhook", prompt: "Build a Stripe webhook handler that processes subscription lifecycle events securely." },
	{ label: "Rate limit + Redis", prompt: "Implement a global rate limiting middleware using Redis and returning proper 429 headers." },
	{ label: "Serverless cron", prompt: "Create a scheduled background task that cleans up soft-deleted records older than 30 days." },
];
